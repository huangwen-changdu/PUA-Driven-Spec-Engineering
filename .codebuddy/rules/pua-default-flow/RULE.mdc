---
description: 全局默认 AI 协作流程（PUA-Driven Spec Engineering）
alwaysApply: true
enabled: true
provider:
---

# 仓库协作规范（PUA-Driven Spec Engineering）

> **注意（CodeBuddy 用户）**：本 RULE.mdc 必须与项目根目录的 `CLAUDE.md` 同时存在，才能稳定触发 PUA 流程。仅有 RULE.mdc 不含完整上下文，或仅有 CLAUDE.md 未配置规则，均可能导致流程不触发。

## 默认技能流程

**本仓库默认使用 `superpowers-pua` 技能套件。** 每次对话开始时，第一个动作必须是 `use_skill("using-superpowers-pua")`，由其完成接任务、门禁判断和技能路由。普通问题走 G0/G1 轻量快放，高压问题走完整 PUA 流程。

## PSE 核心纪律

### 三维自适应门禁

`pua-gate` 按三个维度动态定档，**最终档位 = max(成熟度档位, 变更风险档位) + 复合升级**：

- **需求成熟度**（0-10 分）：目标/约束/影响面/唯一性/实现路径，每项 0-2 分
- **变更风险等级**（R0-R4）：数据持久化/资金权益/权限认证/核心流程/跨服务/数据迁移
- **复合升级**：高风险(R2+) + 不清晰(成熟度≤6) + 大影响(3+文件/跨模块)，命中 2+ 维度额外升档

### 风险等级快速设定

用户可在需求前直接声明风险等级，免去 AI 自动评估：

```
R0: [需求描述]   # 无风险，直接走 writing-plans
R1: [需求描述]   # 低风险，增量新增
R2: [需求描述]   # 中风险，需进入 brainstorming-pua
R3: [需求描述]   # 高风险，需 ESCALATE + 完整设计文档
R4: [需求描述]   # 关键风险，BLOCKED 直到影响面报告完整
```

AI 收到显式风险标记后，直接按该等级定调，跳过自动风险评估流程。

### OpenSpec SDD 四层渐进确认

变更风险 ≥ R2 时，brainstorming-pua 按 OpenSpec 规范驱动开发流程推进，**做一步确认一步**：

1. **Proposal**（`openspec/changes/{name}/proposal.md`）：为什么改、改什么、影响谁 → 用户确认
2. **路径选择**（Proposal 确认后）：选 A（完整 Specs 深度澄清）或 B（直接 writing-plans 快速路径）→ 用户选择
3. **Specs**（`openspec/changes/{name}/specs/spec.md`）：需求边界、验收标准、非目标 → 用户确认（仅选 A 时执行）
4. **Design**（`openspec/changes/{name}/design.md`）：方案对比、技术设计、接口定义 → 用户确认（R3+ 含回滚方案）
5. **Tasks**（`openspec/changes/{name}/tasks.md`）：实现清单、执行顺序、验证计划 → 用户确认

**前一层未确认，后一层无法正确生成。不允许一次输出全部四层文档。**

### 多轮澄清协议

需求不清楚时，模糊点列入澄清队列逐项推进；3-4 轮仍有核心模糊点则升档；5+ 轮则 ESCALATE + 给出结构化选项。

### 文档落地

R2+ 变更必须有 OpenSpec 文档落地，没有文档不允许开写。变更完成后归档至 `openspec/changes/archive/{date}-{name}/`。

## 编码行为准则（karpathy-guidelines）

写代码、review 或重构时必须遵守：

1. **先想后写**：声明假设，不确定时停下问；多种解读时列出来，不默默选一个
2. **简单优先**：最小代码解决问题。没被要求的功能不加，没被要求的抽象不做
3. **外科手术式修改**：只动被要求动的。不顺手改相邻代码，不重构没坏的东西，匹配现有风格
4. **目标驱动**：先定可验证的成功标准。"修好 bug" → 先写复现测试，再让它通过

## 用户硬性规则

- 默认使用简体中文；代码、命令、路径、标识符、接口名、错误信息保持原文。
- 修改中文文件先评估编码风险；最小范围修改，不无故中英转换或重写整文件。
- 只补必要注释（业务规则、边界条件、关键实现原因）；优先小范围可审查补丁。

## 默认执行习惯

- 除非遇到真实阻塞，否则不要反复询问是否继续。
- 默认连续执行到可交付结果；仅在真正模糊、危险或破坏性操作前提问。

## 底线

**先技能后行动；先查后问；先证据后结论；一旦高压信号出现，立刻调用 `pua-escalation` 收紧流程。**
